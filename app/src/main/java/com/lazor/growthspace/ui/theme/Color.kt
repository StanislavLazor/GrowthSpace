package com.lazor.growthspace.ui.theme

import androidx.compose.ui.graphics.Color

// ==========================================
//      ОСНОВНІ КОЛЬОРИ БРЕНДУ (Primary)
// ==========================================

// Неоновий блакитний
// Головні кнопки ("Увійти", "Забронювати"), прогрес-бар (74%),
// Активні іконки нижнього меню, обрані дати/час у календарі, перемикач ролей "Клієнт".
val PrimaryBlue = Color(0xFF3DAEFF)

// Темніший синій
// Для ефекту натискання (ripple/pressed state) на головних блакитних кнопках, щоб вони реагували на клік.
val PrimaryBlueDark = Color(0xFF007BFF)

// ==========================================
//    КОЛЬОРИ ФОНУ (Background & Surface)
// ==========================================

// Найтемніший колір (чорно-синій)
// Фон абсолютно всіх екранів (підкладка додатка).
val BackgroundDark = Color(0xFF0D1117)

// Світліший темний (колір поверхні)
// Фон усіх карток (картки коучів, картки сесій, плашка "З поверненням", бульбашки чату).
val SurfaceDark = Color(0xFF161B22)

// Найбільш виділений темний (Elevated)
// Поля вводу (Email, Пароль), неактивні чипси-фільтри ("Бізнес", "Life-coach"),
// неактивні слоти часу.
val SurfaceDarkElevated = Color(0xFF21262D)

// ==========================================
//   ТЕКСТ ТА ІКОНКИ (Typography & Icons)
// ==========================================

// Основний білий
// Головні заголовки ("Створити акаунт", "Мої сесії"), імена коучів, введений користувачем текст.
val TextWhite = Color(0xFFFFFFFF)

// Сірий колір
// Підзаголовки, плейсхолдери в полях вводу ("Ім'я", "Пароль"), неактивні іконки в навбарі.
val TextGray = Color(0xFF8B949E)

// Блакитний текстовий
// Клікабельні посилання ("Забули пароль?", "Умовами використання", "Змінити").
val TextBlue = Color(0xFF3DAEFF)

// ==========================================
//       СТАТУСИ ТА ІНДИКАТОРИ (Badges)
// ==========================================

// Жовтий
// Бейдж "Очікується", велика іконка годинника при очікуванні підтвердження сесії.
val StatusPending = Color(0xFFEAB308)

// Зелений
// Бейдж "Підтверджено", зелена крапка "онлайн" на аватарках коучів.
val StatusConfirmed = Color(0xFF22C55E)

// Червоний
// Бейдж "Скасовано", іконка та текст кнопки "Вийти з акаунту" в налаштуваннях.
val StatusCanceled = Color(0xFFEF4444)

// Сірий/Приглушений
// Бейдж "Завершено" на вкладці минулих сесій.
val StatusCompleted = Color(0xFF6B7280)